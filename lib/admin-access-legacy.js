'use strict';

// Temporary placeholder replaced in the next commit with the exact pre-command
// admin-access implementation. Keeping the legacy flow separate lets the new
// /akses dispatcher stay tiny while preserving the already-tested fallback.
module.exports = {};
