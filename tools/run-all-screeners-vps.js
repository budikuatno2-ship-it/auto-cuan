#!/usr/bin/env node
'use strict';
// VPS-only manual orchestration. Read-only unless --execute is explicit.
const fs = require('node:fs'); const path = require('node:path');
const ENV_FILES = ['.env.local', '.env.intraday-runtime', '.env'];
function loadEnvFiles(env = process.env, cwd = process.cwd()) { for (const file of ENV_FILES) { let text; try { text = fs.readFileSync(path.join(cwd, file), 'utf8'); } catch (e) { if (e.code === 'ENOENT') continue; throw e; } text.split(/\r?\n/).forEach((line) => { const m=line.match(/^\s*(?:export\s+)?([A-Za-z_][\w]*)\s*=\s*(.*?)\s*$/); if (!m || Object.prototype.hasOwnProperty.call(env,m[1])) return; let v=m[2]; if ((v.startsWith('"')&&v.endsWith('"'))||(v.startsWith("'")&&v.endsWith("'"))) v=v.slice(1,-1); else v=v.replace(/\s+#.*$/,''); env[m[1]]=v; }); } return env; }
function parseArgs(argv) { const o={dryRun:true,execute:false,send:false,force:false,resumeStale:false,limit:50,nkBatchSize:8,sleepMs:1500,maxAttempts:40}; for(let i=2;i<argv.length;i++){const a=argv[i]; if(a==='--dry-run'){o.dryRun=true;} else if(a==='--execute'){o.execute=true;o.dryRun=false;} else if(a==='--send'){o.send=true;} else if(a==='--force')o.force=true; else if(a==='--resume-stale')o.resumeStale=true; else if(a==='--include-intraday-dry-run')o.includeIntradayDryRun=true; else if(a==='--skip-swing')o.skipSwing=true; else if(/^--skip-(konglo|nk|daytrade|top5|progress)$/.test(a))o[a.slice(7)]=true; else if(['--limit','--nk-batch-size','--sleep-ms','--max-attempts'].includes(a)){const k=a==='--sleep-ms'?'sleepMs':a==='--max-attempts'?'maxAttempts':a==='--nk-batch-size'?'nkBatchSize':'limit';o[k]=Math.max(1,Number(argv[++i])||o[k]);} else throw new Error('Unknown option: '+a);} if (![8,25,50].includes(o.nkBatchSize)) throw new Error('--nk-batch-size must be 8, 25, or 50'); return o; }
function wibDate(now=new Date()){return new Date(now.getTime()+25200000).toISOString().slice(0,10);}
function finalStatus(value){return ['PUBLISHED','DAILY','COMPLETED','COMPLETED_NO_CANDIDATES','ALREADY_DONE'].includes(String(value||'').toUpperCase());}
function publishedToday(meta,today=wibDate()){return !!meta&&finalStatus(meta.status)&&String(meta.run_date||meta.calculated_at||'').slice(0,10)===today;}
function finalizedResponse(data){return !!data&&(finalStatus(data.status)||String(data.step||'').toLowerCase()==='finalize'||/published\s+\d+\s+top candidates/i.test(String(data.message||'')));}
function staleScanning(meta, now=Date.now()){if(String(meta&&meta.status||'').toLowerCase()!=='scanning')return false; const stamp=Date.parse(meta.updated_at||meta.calculated_at||''); return Number.isFinite(stamp)&&now-stamp>30*60*1000;}
function sleep(ms){return new Promise(r=>setTimeout(r,ms));}
function makeClient(baseUrl,secret,fetchImpl=fetch){async function call(query){const url=new URL('/api/sector-hot',baseUrl);Object.entries(query).forEach(([k,v])=>url.searchParams.set(k,String(v)));const r=await fetchImpl(url,{headers:{Authorization:'Bearer '+secret,Accept:'application/json'}});const text=await r.text();let data;try{data=text?JSON.parse(text):{};}catch(e){throw new Error('Invalid JSON '+url);}if(!r.ok)throw new Error('HTTP '+r.status+' '+url);return data;}return {call};}
async function nkStatus(client){return client.call({action:'nk-screener-results'});}
async function runNk(client,opts,log=console.log){let status=await nkStatus(client), meta=status.meta||{}; if(publishedToday(meta)&&!opts.force){log('Non-Konglo: terminal today; skipped.');return {skipped:true,status};} if(!opts.execute){log('Non-Konglo: read-only; nk-screener-run not called.');return {planned:true,status};} if(staleScanning(meta)&&!opts.resumeStale&&!opts.force){log('Non-Konglo: STALE SCAN; pass --resume-stale or --force.');return {stale:true,status};} for(let attempt=1;attempt<=opts.maxAttempts;attempt++){if(finalStatus(meta.status))return {finalized:true,status}; const q={action:'nk-screener-run'};if(opts.force&&attempt===1)q.force=1;if(opts.nkBatchSize!==8&&attempt===1)q.batch_size=opts.nkBatchSize;const response=await client.call(q);log('Non-Konglo attempt '+attempt+': '+(response.step||response.status||response.message||'ok'));if(finalizedResponse(response))return {finalized:true,response};status=await nkStatus(client);meta=status.meta||{};if(finalStatus(meta.status))return {finalized:true,status};await sleep(opts.sleepMs);}throw new Error('Non-Konglo reached --max-attempts without finalization; refusing to restart.');}
async function ready(client,action){const data=await client.call({action});return {data,meta:data.meta||{}};}
async function runDayTrade(client,opts,log=console.log){let status=await ready(client,'daytrade-screener');if(publishedToday(status.meta)&&Number(status.meta.scanned_count||0)>=Number(status.meta.universe_count||0)&&!opts.force)return {skipped:true,status};if(!opts.execute){log('Day Trade: read-only; run endpoint not called.');return {planned:true,status};}for(let batch=0;batch<opts.maxAttempts;batch++){const r=await client.call({action:'daytrade-screener-run',batch,limit:opts.limit,...(opts.force&&batch===0?{force:1}:{})});log('Day Trade batch '+(batch+1)+': '+(r.status||r.message||'ok'));if(finalStatus(r.status)||['already_done','completed','published'].includes(String(r.status||'').toLowerCase()))return {finalized:true,response:r};if(String(r.status||'').toLowerCase()!=='running')return {stopped:true,response:r};await sleep(opts.sleepMs);}throw new Error('Day Trade reached --max-attempts without completion; refusing to restart.');}
async function main(options=parseArgs(process.argv),deps={}){const env=deps.env||loadEnvFiles();env.DAYTRADE_INTRADAY_SCORE_ENABLED='false';if(!env.CRON_SECRET)throw new Error('CRON_SECRET is required.');const base=env.APP_BASE_URL||'https://auto-cuan.vercel.app',client=deps.client||makeClient(base,env.CRON_SECRET,deps.fetch),log=deps.log||console.log;log('Mode: '+(options.execute?'EXECUTE':'DRY-RUN'));log('Telegram: '+(options.send&&options.execute?'ON':'OFF'));log('Intraday production scoring: OFF');log('App base URL: '+base);const swing=await ready(client,'screener');log('[Phase 2] Swing Konglo');if(!options.skipSwing&&!options.konglo&&options.execute&&!(publishedToday(swing.meta)&&!options.force))await client.call({action:'refresh-screener',ai:0,...(options.force?{force:1}:{})});else log('Skipped/read-only: Swing Konglo.');log('[Phase 3] Swing Non-Konglo');if(!options.skipSwing&&!options.nk)await runNk(client,options,log);else log('Skipped: --skip-swing or --skip-nk.');log('[Phase 4] Day Trade');if(!options.daytrade)await runDayTrade(client,options,log);else log('Skipped: --skip-daytrade.');log('[Phase 5] Top 5');const check=await client.call({action:'telegram-daily-picks',lock_only:1,dry_run:1});const blocked=check.reason==='screeners_not_ready'||check.reason==='not_ready'||check.ready===false||check.status==='scanning';if(!options.top5&&!blocked&&options.execute)await client.call({action:'telegram-daily-picks',...(options.send?{}:{dry_run:1})});else log('Skipped/read-only: Top 5 generation.');log('[Phase 6] Top 5 Progress: '+(options.send&&options.execute?'send permitted':'dry-run only')+'.');if(options.includeIntradayDryRun)log('[Phase 7] Intraday validation: observe/dry-run only; production scoring remains OFF.');return {base_url:base,dry_run:!options.execute};}
if(require.main===module)main().catch(e=>{console.error(e.message);process.exitCode=1;});
module.exports={ENV_FILES,loadEnvFiles,parseArgs,wibDate,finalStatus,publishedToday,finalizedResponse,staleScanning,makeClient,nkStatus,runNk,runDayTrade,main};
